import { FdirDirective } from './fdir.directive';

describe('FdirDirective', () => {
  it('should create an instance', () => {
    const directive = new FdirDirective();
    expect(directive).toBeTruthy();
  });
});
